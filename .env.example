# Required
DISCORD_TOKEN=
DISCORD_CLIENT_ID=

# Optional but recommended for fast command iteration (guild commands update instantly)
DISCORD_GUILD_ID=
# Optional: override the Family Leader role ID (defaults to production role)
FAMILY_LEADER_ROLE_ID=

# Optional
LOG_LEVEL=info

# Optional: Clash of Clans API request timeout (ms)
CLASH_OF_CLANS_API_TIMEOUT_MS=10000
